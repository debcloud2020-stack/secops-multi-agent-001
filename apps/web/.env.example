# Base URL of the FastAPI backend the dashboard polls. Inlined at BUILD time (NEXT_PUBLIC_*).
# Local dev: localhost. Phase 5b-2 deploy: the GitHub frontend workflow sets this to the
# deployed Container Apps URL (the `BACKEND_URL` repo variable) before `npm run build`.
NEXT_PUBLIC_API_URL=http://localhost:8000
